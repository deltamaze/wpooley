# look at the error folder on parent directory
# look for any file created in past 48 hours
# if file exist, send email with filename and file contents
# pull email config from a file that is it the gitignore
